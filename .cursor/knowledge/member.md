# Member App

- Pages: `src/pages/member/` — dashboard, history, profile
- WO interfaces: `src/utils/interfaces/IWo.ts`
- History display: dayjs format (belum pakai `formatWorkOrderDateTime` admin)

Cross-repo API: consume backend customer endpoints di `backend/apps/backend/src/api/customers/`
